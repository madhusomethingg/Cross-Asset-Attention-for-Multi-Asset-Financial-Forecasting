# =============================================================================
# Streamlit Cloud deployment requirements
#
# Slim runtime list -- only what the dashboard actually loads. No training-only
# deps. torch is pulled from the CPU-only wheel index (smaller + Streamlit
# Cloud has no GPU anyway).
# =============================================================================
--extra-index-url https://download.pytorch.org/whl/cpu

# Core scientific stack
numpy>=1.26                 # latest yfinance requires >=1.26; no upper bound needed
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3

# Deep learning (CPU build)
torch>=2.1

# Data acquisition
yfinance>=0.2.40

# Plotting + UI
plotly>=5.20
streamlit>=1.32
matplotlib>=3.7             # required by pandas Styler.background_gradient()

# Utilities
pyyaml>=6.0
